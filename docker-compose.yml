services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./vector_store:/app/vector_store
    environment:
      LLM_PROVIDER: mock
      DATABASE_URL: sqlite:///./data/operations.db
      VECTOR_STORE_PATH: ./vector_store
      LOG_LEVEL: INFO
